<?php
  // create a connection the database engine
  $db = mysql_connect("127.0.0.1", "secretagent", "<%=@db_password%>");
  if(!$db)
    die("Couldn't connect to the MySQL server.");

  // change database
  $use = mysql_select_db("secretagent", $db);
  if(!$use)
    die("Couldn't select database.");
?>
